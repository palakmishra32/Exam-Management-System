package com.exam.exam_backend.repository;

import com.exam.exam_backend.entity.RevealRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RevealRequestRepository extends JpaRepository<RevealRequest, Long> {
}
